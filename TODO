Check out Math::Int64 for better ways to do 64-bit stuff

Win32 support

Improve varint performance, buffer_read/write_varint, etc

Provide higher-performance versions using XS::MemoryBuffer of:
  BufferedTransport
  FramedTransport
  HttpClient

Return int counts from CompactProtocol methods.

Fix/hide "warning: null argument where non-null required (argument 1)"
  because of calls to croak(NULL), a valid usage.
